# powstock.entities — Entity resolution (DEAD CODE)

This package contains entity resolution code that is currently unused.
Both files are marked DEAD and have known issues.

- `resolver.py` — Cross-company director graph (never imported)
- `registry.py` — Duplicate of universe.py (has syntax error)
